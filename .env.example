# Google Calendar API Configuration (Web/Android only)
#
# Note: iOS uses Apple Reminders natively and doesn't require Google OAuth
#
# Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Google Calendar API
# 4. Create credentials:
#    - OAuth 2.0 Client ID
#      - Application type: Web application
#      - Authorized JavaScript origins: http://localhost:5173 (for development)
#      - Copy the Client ID to VITE_GOOGLE_CLIENT_ID
#    - API Key (for Calendar API)
#      - Copy to VITE_GOOGLE_API_KEY
# 5. Copy this file to .env and replace the values with your actual credentials

# OAuth 2.0 Client ID (Web application)
VITE_GOOGLE_CLIENT_ID=your-web-client-id.apps.googleusercontent.com

# API Key (for Calendar API)
VITE_GOOGLE_API_KEY=your-api-key-here
